{
  "buildCommand": "cd ../.. && pnpm -C packages/database exec prisma generate && pnpm build:packages && cd apps/web && pnpm build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
